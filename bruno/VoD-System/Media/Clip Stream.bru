meta {
  name: Clip Stream
  type: http
  seq: 3
}

post {
  url: {{base_url}}/media/clip
  body: json
  auth: inherit
}

body:json {
  {
    "duration": 60.0,
    "title": "Hello",
    "description": "my clip"
  }
}

script:post-response {
  bru.setEnvVar("uuid", res.getBody().data.uuid);
}
