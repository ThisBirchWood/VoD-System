meta {
  name: Create Marker
  type: http
  seq: 2
}

post {
  url: {{base_url}}/markers
  body: json
  auth: inherit
}

body:json {
  {
    "message": "Nice play"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
